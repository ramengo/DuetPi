--- stage-fusion.orig/rootfs/etc/usbmount/usbmount.conf
+++ stage-fusion/rootfs/etc/usbmount/usbmount.conf
@@ -14,7 +14,7 @@ MOUNTPOINTS="/media/usb0 /media/usb1 /me
 
 # Filesystem types: removable storage devices are only mounted if they
 # contain a filesystem type which is in this list.
-FILESYSTEMS="vfat ext2 ext3 ext4 hfsplus"
+FILESYSTEMS="vfat ext2 ext3 ext4 hfsplus ntfs"
 
 #############################################################################
 # WARNING!                                                                  #
@@ -46,7 +46,7 @@ MOUNTOPTIONS="sync,noexec,nodev,noatime,
 # For example, "-fstype=vfat,gid=floppy,dmask=0007,fmask=0117" would add
 # the options "gid=floppy,dmask=0007,fmask=0117" when a vfat filesystem
 # is mounted.
-FS_MOUNTOPTIONS=""
+FS_MOUNTOPTIONS="-fstype=ntfs,nls=utf8,uid=dsf,gid=users,dmask=0007,fmask=0117 -fstype=vfat,uid=dsf,gid=users,dmask=0007,fmask=0117"
 
 # If set to "yes", more information will be logged via the syslog
 # facility.
